class {{ name }}(pdef.Enum):
{% if doc %}
    """{{ doc|pydoc|indent(4) }}"""

{% endif %}
{% for value in values %}
    {{ value.name }} = '{{ value.name }}'
{% endfor %}

    DESCRIPTOR = pdef.descriptors.enum(lambda: {{ name }},
        values=[{% for value in values -%}
            {{ value.name }}
            {%- if not loop.last %}, {% endif %}
        {%- endfor %}])

package {{package}};

{% if doc %}
/** {{ doc }} */
{% endif %}
public enum {{name}} {
    {% for value in values -%}
        {{value}}{% if not loop.last and loop.length > 1 %}, {% endif %}
    {%- endfor %};

    public static final {{ name }} instance = {{ values[0] }};
    public static final io.pdef.Descriptor<{{name}}> descriptor = io.pdef.Descriptors.enum0(instance);
}

